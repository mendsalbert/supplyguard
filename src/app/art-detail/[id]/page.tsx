"use client";
import React, { FC, useState } from "react";

const ProductDetailPage = ({ params }: { params: { id: number } }) => {
  return <div className={`nc-ProductDetailPage `}>{/* MAIn */}</div>;
};

export default ProductDetailPage;
